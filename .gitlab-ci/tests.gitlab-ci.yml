tests:
  stage: tests
  image: ${CI_REGISTRY_IMAGE}
  script:
    - build/bin/leetcode-solutions-tests --reporter=junit --out=tests_report.xml
  artifacts:
    when: always
    reports:
      junit: tests_report.xml
  interruptible: true
